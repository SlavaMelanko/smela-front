import './styles.scss'

import { LayoutDashboard } from 'lucide-react'

import BaseIcon from '../Icon'

const DashboardIcon = props => (
  <BaseIcon {...props} icon={LayoutDashboard} name='dashboard' />
)

export default DashboardIcon

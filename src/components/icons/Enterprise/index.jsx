import './styles.scss'

import { Building2 } from 'lucide-react'

import BaseIcon from '../Icon'

const EnterpriseIcon = props => (
  <BaseIcon {...props} icon={Building2} name='enterprise' />
)

export default EnterpriseIcon

import './styles.scss'

import { CreditCard } from 'lucide-react'

import BaseIcon from '../Icon'

const CreditCardIcon = props => (
  <BaseIcon {...props} icon={CreditCard} name='credit-card' />
)

export default CreditCardIcon

import './styles.scss'

import { RefreshCw } from 'lucide-react'

import BaseIcon from '../Icon'

const RefreshIcon = props => (
  <BaseIcon {...props} icon={RefreshCw} name='refresh' />
)

export default RefreshIcon

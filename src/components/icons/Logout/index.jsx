import './styles.scss'

import { LogOut } from 'lucide-react'

import BaseIcon from '../Icon'

const LogoutIcon = props => <BaseIcon {...props} icon={LogOut} name='logout' />

export default LogoutIcon

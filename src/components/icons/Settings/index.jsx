import './styles.scss'

import { Settings2 } from 'lucide-react'

import BaseIcon from '../Icon'

const SettingsIcon = props => (
  <BaseIcon {...props} icon={Settings2} name='settings' />
)

export default SettingsIcon

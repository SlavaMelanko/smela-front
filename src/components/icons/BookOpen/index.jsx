import './styles.scss'

import { BookOpen } from 'lucide-react'

import BaseIcon from '../Icon'

const BookOpenIcon = props => (
  <BaseIcon {...props} icon={BookOpen} name='book-open' />
)

export default BookOpenIcon

import './styles.scss'

import { ListFilter } from 'lucide-react'

import BaseIcon from '../Icon'

const FilterIcon = props => (
  <BaseIcon {...props} icon={ListFilter} name='filter' />
)

export default FilterIcon

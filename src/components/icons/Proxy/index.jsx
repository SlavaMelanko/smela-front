import './styles.scss'

import { MapPinCheckInside } from 'lucide-react'

import BaseIcon from '../Icon'

const ProxyIcon = props => (
  <BaseIcon {...props} icon={MapPinCheckInside} name='proxy' />
)

export default ProxyIcon

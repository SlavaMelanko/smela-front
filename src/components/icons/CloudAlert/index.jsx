import './styles.scss'

import { CloudAlert } from 'lucide-react'

import BaseIcon from '../Icon'

const CloudAlertIcon = props => (
  <BaseIcon {...props} icon={CloudAlert} name='cloud-alert' />
)

export default CloudAlertIcon
